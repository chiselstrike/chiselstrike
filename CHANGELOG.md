# ChiselStrike Changelog

## 0.1.0

Added:

- Endpoint data access support via `Chisel.find_all` and `Chisel.store` JavaScript APIs
- Endpoint definitions using JavaScript.
- Type definitions using GraphQL schema format.
