# SPDX-FileCopyrightText: © 2021 ChiselStrike <info@chiselstrike.com>

# RUN: sh -e @file

mkdir -p "$TEMPDIR/types"

cat << EOF > "$TEMPDIR/types/types.graphql"
  type TestLabelsPersist1 {
    one: String @a @b
    two: String @a
  }

  type TestLabelsPersist2 {
    three: String @c @b
    four: String @a
  }
EOF

cd "$TEMPDIR"
$CHISEL apply

$CHISEL restart

$CHISEL type export

# CHECK: one: String @a @b
# CHECK: two: String @a
# CHECK: three: String @c @b
# CHECK: four: String @a
