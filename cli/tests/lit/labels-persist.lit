# SPDX-FileCopyrightText: © 2021 ChiselStrike <info@chiselstrike.com>

# RUN: sh -e @file

cat << EOF > "$TEMPDIR/types/types.graphql"
  class TestLabelsPersist1 {
    @a @b one: string;
    @a two: string;
  }

  class TestLabelsPersist2 {
    @c @b three: string;
    @a four: string;
  }
EOF

cd "$TEMPDIR"
$CHISEL apply

$CHISEL restart

$CHISEL describe

# CHECK: @a @b one: string;
# CHECK: @a two: string;
# CHECK: @c @b three: string;
# CHECK: @a four: string;
