# SPDX-FileCopyrightText: © 2021 ChiselStrike <info@chiselstrike.com>

# RUN: sh -e @file

echo 'class TestLabels {@a @b foo: string; }' > "$TEMPDIR/types/types.ts"

cd "$TEMPDIR"
$CHISEL apply

$CHISEL type export

# CHECK: @a @b foo: string;
