# SPDX-FileCopyrightText: © 2021 ChiselStrike <info@chiselstrike.com>

# RUN: sh -e @file

mkdir -p "$TEMPDIR/types"

echo 'type TestLabels {foo: String @a @b}' > "$TEMPDIR/types/types.graphql"

cd "$TEMPDIR"
$CHISEL apply

$CHISEL type export

# CHECK: foo: String @a @b
