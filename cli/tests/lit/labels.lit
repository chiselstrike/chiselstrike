# SPDX-FileCopyrightText: © 2021 ChiselStrike <info@chiselstrike.com>

# RUN: sh -e @file

echo 'class TestLabels {@labels("a", "b") foo: string; }' > "$TEMPDIR/types/types.ts"

cd "$TEMPDIR"
$CHISEL apply
$CHISEL describe

# CHECK: @labels("a", "b") foo: string;

echo 'class TestLabels {@labels("a") @labels("b") foo: string; }' > "$TEMPDIR/types/types.ts"

cd "$TEMPDIR"
$CHISEL apply
$CHISEL describe
# CHECK: @labels("a", "b") foo: string;

echo 'class TestLabels {@labels foo: string; }' > "$TEMPDIR/types/types.ts"
cd "$TEMPDIR"
$CHISEL apply 2>&1 || echo -
# CHECK: Error: expected a call-like decorator

echo 'class TestLabels {@labels(123) foo: string; }' > "$TEMPDIR/types/types.ts"
cd "$TEMPDIR"
$CHISEL apply 2>&1 || echo -
# CHECK: Error: Only strings accepted as labels
#
echo 'class TestLabels {@not_a_label("labels") foo: string; }' > "$TEMPDIR/types/types.ts"
cd "$TEMPDIR"
$CHISEL apply 2>&1 || echo -
# CHECK: Error: Only the 'labels' decorator is supported by ChiselStrike
